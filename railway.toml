# railway.toml
# Configuración para Railway
# Railway lee este archivo para saber cómo arrancar el servicio

[build]
builder = "DOCKERFILE"

[deploy]
startCommand = "uvicorn main:app --host 0.0.0.0 --port 8000"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
